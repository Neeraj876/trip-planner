apiVersion: v1
kind: Service
metadata:
  name: trip-planner-backend-headless
spec:
  clusterIP: None
  selector:
    app: trip-planner-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
